@inherits LayoutComponentBase

@*<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
        <div class="top-row px-4">
            <a href="https://learn.microsoft.com/aspnet/core/" target="_blank">About</a>
        </div>

        <article class="content px-4">
            @Body

            @* Required 
            <MudThemeProvider />
            <MudPopoverProvider />

            @* Needed for dialogs 
            <MudDialogProvider
                Position="DialogPosition.Center"
                CloseButton="true"
                CloseOnEscapeKey="true"
                BackdropClick="false" />

            @* Needed for snackbars 
            <MudSnackbarProvider />
        </article>
    </main>
</div>*@

<div class="layout-container">
    <!-- Sidebar a sinistra (Navbar attuale) -->
    <div class="left-sidebar">
        <NavMenu />
    </div>

    <!-- Contenuto centrale -->
    <div class="main-content">
        <div class="top-row px-4">
            <a href="https://learn.microsoft.com/aspnet/core/" target="_blank">About</a>
        </div>

        <article class="content px-4">
            @Body

            <MudThemeProvider />
            <MudPopoverProvider />
            <MudDialogProvider Position="DialogPosition.Center"
                               CloseButton="true"
                               CloseOnEscapeKey="true"
                               BackdropClick="false" />
            <MudSnackbarProvider />
        </article>
    </div>

    <!-- Server sidebar a destra -->
    <div class="right-sidebar">
        <ServerSidebar />
    </div>
</div>